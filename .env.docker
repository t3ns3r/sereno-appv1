# Variables de entorno para Docker
# Copia este archivo a .env y personaliza los valores

# Contraseñas (CAMBIAR POR VALORES SEGUROS)
DB_PASSWORD=Sereno2024SecureDB!
REDIS_PASSWORD=Sereno2024SecureRedis!
JWT_SECRET=tu-jwt-secret-super-seguro-de-64-caracteres-minimo-para-produccion-2024

# Configuración de la aplicación
FRONTEND_URL=https://tu-dominio.com
NODE_ENV=production

# Base de datos (se configura automáticamente con Docker)
DATABASE_URL=postgresql://sereno_user:Sereno2024SecureDB!@postgres:5432/sereno_db
REDIS_URL=redis://:Sereno2024SecureRedis!@redis:6379

# APIs externas (configurar cuando las necesites)
GOOGLE_SPEECH_API_KEY=
OPENAI_API_KEY=
GOOGLE_MAPS_API_KEY=

# Push notifications (opcional)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=tu-email@ejemplo.com